# Daily OS · 设计规范

"Paper & Mint" —— Daily OS 在 macOS 与 iOS 上的视觉与交互规范。

这份文档描述**决策和理由**，token 的唯一真相源是代码：
`Sources/DailyOSCore/DesignSystem/`。两边不一致时以代码为准，并回来改这份文档。

**范围**：这是两端共用的设计规范，跟 `DailyOSCore` 一起住在 macOS 仓库里。
iOS 仓库（[alexli-77/daily-os-ios](https://github.com/alexli-77/daily-os-ios)）依赖同一个包、
遵循同一份文档——**不要在那边复制一份**，一式两份的规范在第一次改色板时就会分家。
§7 是两端差异清单，写在这里是为了让差异是一处明确的决定，而不是两边各自演化的结果。

---

## 1. 产品形态

Web 控制台有九个页面加一个九节的配置台。搬到原生 App 时不是逐页平移，做了三个删减：

| Web | 原生 | 理由 |
| --- | --- | --- |
| `/dashboard` | **删除**，并入「今天」顶部状态条 | 它是「今日 workflow + 最近运行 + token 用量」三件不相关的事叠在一起，因为浏览器标签页有空间。运行有自己的页，剩下的是一条状态条。 |
| `/today` | 「今天」 | 首屏。最高频的动作不是读，是**把脑子里的一条扔出去**，所以快捷捕获在计划之上。 |
| `/cycles` | 「周期」 | 产品重心，见 §5。 |
| `/okr` | 「OKR」 | 只读为主。写复盘时要能看到当初说要做什么。 |
| `/chat` | 「对话」 | 工具调用轨迹是气泡的一部分，不是日志页里的东西，见 §6。 |
| `/schedules` `/runs` `/artifacts` | 同名，归入侧栏「系统」组 | 出问题时才看的东西。 |
| `/profile` | 并入设置 | 一个只有三行的页面不值得占导航位。 |
| `/console`（九节配置） | macOS「设置」；iOS 只读 | 没人在手机上粘贴 API key。 |

### macOS

两栏 `NavigationSplitView`：源列表 + 内容。

不是三栏。周期 / 运行 / 产物 / 对话确实需要 list-detail，但**由各自的屏幕在内部拆**——
全局三栏会让「今天」「OKR」「设置」多出一个没有意义的中间栏，而一个时有时无的栏，
其折叠行为比没有这一栏更糟。

- 侧栏分两组：**工作**（今天 / 周期 / OKR / 对话）、**系统**（运行 / 产物 / 排程）
- 侧栏底部常驻：头像、服务状态灯、设置入口
- 菜单栏项（`MenuBarExtra`）：状态灯 + 快捷捕获 + 今日前四条 + 打开主窗口
- ⌘1…⌘7 切换 section，⌘, 进设置

**关于菜单栏 vs 桌面浮窗**：上一版原型两个形态都做了——菜单栏徽标 + 屏幕右缘半隐藏的企鹅。
浮窗解决的是真问题（菜单栏挤满时状态项会被隐藏），但制造了更糟的问题：一个永远置顶、
跟着你跨 Space、不退出应用就赶不走的窗口。**这一版只保留菜单栏形态。**
企鹅作为品牌资产保留（关于页 / 空状态 / 图标），不再作为常驻窗口。

### iOS

三个 tab + 「更多」：**今天 / 周期 / 对话 / 更多**。

Tab 位留给「做事」的界面：今天有什么、这一期到哪了、问一句。
运行 / 产物 / 排程 / 设置放进「更多」——那些是「感觉哪里不对」时才去的地方，不值得占拇指位。

手机端**不做配置**。没有 provider 切换、没有密钥输入、没有 skill 安装，只显示 Mac 上是什么设置。

---

## 2. 色彩

「Paper & Mint」——暖灰米的纸，纸上只有一条薄荷色阶。

取代了上一版「Paper & Moss」的两级强调色（`moss` / `mossSoft`）。今天页需要说清**多少薄荷**
——悬停淡底、chip 填充、选中行、实心圆点、超时那行字——两级撑不起六件事，
总有一件要借一个它不该用的颜色。

| Token | Light | Dark | 用途 |
| --- | --- | --- | --- |
| `paper` | `#F0EFEB` | `#17191A` | 页面底 |
| `page` | `#FAFAF8` | `#1F2223` | 纸面，写着这一天的那张纸 |
| `rule` | `#E2E1DB` | `#2F3335` | 行间分隔线、纸的描边 |
| `ink` | `#2C2C30` | `#E8EAEA` | 正文 |
| `ink2` | `#63646A` | `#AEB2B4` | 时段、次要文字 |
| `ink3` | `#727377` | `#83898C` | 说明文字、已结束的行、空圈描边 |
| `mint50` | `#EDF7F3` | `#1D2925` | 圆圈 hover 底 |
| `mint100` | `#D6EEE4` | `#22352F` | chip hover、图表淡柱 |
| `mint200` | `#B4E0D0` | `#2B4A40` | 主按钮底、选中态底 |
| `mint400` | `#7FCCB3` | `#4CA48A` | 完成圆点、进度条、现在线 |
| `mint600` | `#287963` | `#86D2BC` | 超时文字、链接 hover |
| `mint800` | `#1B5245` | `#C9EEE1` | 薄荷底上的字 |
| `q1` | `#B04D4D` | `#E98383` | **只给 MIT** |

### 色阶编码的四条规矩

- `mint400` 是唯一的实心强调色。
- `mint200` 是唯一的淡色底，它上面的字**一律** `mint800`。
- 浅色模式下**没有任何深底白字**。层级靠发丝线和留白，不靠反白块。
- `q1` 只出现在 MIT 标记上。它是整个产品里唯一的暖色相，一天只值一个词。

### 对比度

每个文字 token 都对着它**实际画在的底**、按**实际字号**验算过，四个值为达到 WCAG AA（4.5:1）而调整：

| token | 原值 | 现值 | 底 | 对比度 |
| --- | --- | --- | --- | --- |
| `ink3` light | `#9C9DA3` | `#727377` | `page` | 2.59 → 4.53 |
| `ink3` dark | `#707578` | `#83898C` | `page` | 3.44 → 4.52 |
| `mint600` light | `#2E8A71` | `#287963` | `paper` | 3.66 → 4.56 |
| `q1` light | `#D25C5C` | `#B04D4D` | `page` | 3.70 → 4.52 |

`ink3` 是其中真正要紧的那个：它是**完成态和延期态整行文字**的颜色，2.59:1 意味着做完一天的
奖励是半屏一部分人读不出来的字。弱化并没有因为修正而消失——完成的行还有删除线和实心圆点，
它们不需要靠让眼睛失效来表达「做完了」。

### 系统状态层（明确的例外）

`ok` / `warn` / `danger` / `series` **不受上面的色彩规矩约束**，这是决定，不是遗漏。

上面那套只有一个色相，因为一屏计划应该读起来是连续的一件事。但这个 App 还必须说出
「服务挂了」「密钥缺失」「这次运行失败」——全用薄荷渲染的状态系统一句都说不出来。
让 `q1` 去扛会毁掉让 MIT 标记有意义的那条规矩：它是页面上唯一的暖色标记。

所以状态保留自己的三个颜色，且**只用在整条信息就是一个状态**的地方：服务指示灯、运行结果、
设置校验、登录报错。全 App 84 处，分布在 14 个文件里。**一条计划行永远不是其中任何一个。**

| Token | Light | Dark | 用途 |
| --- | --- | --- | --- |
| `ok` | `#1E7A4D` | `#63C08F` | 成功、已连接、在轨 |
| `warn` | `#A06413` | `#D9A75A` | 有风险、待合入草稿、已停用 |
| `danger` | `#9F2D2D` | `#E08585` | 失败、脱轨、破坏性操作 |
| `series[6]` | 见代码 | | 图表分类色 |

`series` 是同一类例外，且本来就是这么写的：分类数据需要可区分的色相，
否则图例成了读图的唯一途径。

### 重要程度

只有 MIT 有颜色。旧版三级环（赭石 / 琥珀 / 灰）说的是「这三条重要」，
而计划真正做的断言不是这个——prompt 挑的是**一件**最重要的事，
给第 2、3 名上色把那个断言稀释成了一条没人被要求读的渐变。
`high` 和 `normal` 改为靠字重和墨色层级区分，这也是页面其余部分表达「次要」的方式。

**深色不是浅色取反。** 把暖纸白取反会得到一个偏蓝的灰，读起来像另一个品牌。
深色是一套暖近黑，强调色提亮到能在其上保持可读。

**实现方式**：`Color(light:dark:)` 走平台的动态颜色（`UIColor { traits in }` /
`NSColor(name:dynamicProvider:)`），不走 `@Environment(\.colorScheme)`。
环境变量需要一路透传，而在 `MenuBarExtra`、`NSHostingView`、脱离的 popover 里
它会静默地退回浅色——动态颜色由系统在绘制时解析，这些场景直接就是对的。

**深度靠发丝线，不靠阴影。** 全系统没有 shadow。

---

## 3. 字体

**精确字号，并且照样跟随系统字号。**

设计稿写的是固定数值——display 40/48/700，body 14/24/400。照字面写成 `Font.system(size: 40)`
就等于关掉系统自己的文字大小设置：在辅助功能里调大字号的人会发现唯独这个 App 不理他，
静默地，屏幕上没有任何东西解释为什么。

`Font.custom(_:size:relativeTo:)` 两者都给。`size` 是默认设置下的精确点数，
`relativeTo` 是它跟着缩放的那一档。所以下表就是设计稿的数字，而且该长的时候照样长。

| 样式 | 字体 | 字号 / 行高 / 字重 | 缩放基准 | 用途 |
| --- | --- | --- | --- | --- |
| `display` | serif | 40 / 48 / 700 | `.largeTitle` | 日期，每屏一个 |
| `heading` | serif | 16 / 24 / 700 | `.headline` | 区块标题 |
| `body` | sans | 14 / 24 / 400 | `.body` | 任务文字 |
| `label` | sans | 13 / 24 / 500 | `.subheadline` | 时段、按钮 |
| `caption` | sans | 12 / 16 / 400 | `.caption` | 说明、来源、估时 |
| `hand` | 手写 | 34 / 44 / 400 | `.title` | **只在「今天清完了」一处** |

行高写在 `Typo.Leading` 里。SwiftUI 的 `.lineSpacing` 设的是**行间空隙**而不是行盒，
`lineSpacing(for:size:)` 帮视图做掉这个减法。

### 字体

三款都随 macOS 和 iOS 出厂，不打包任何字体文件，也就没有字体授权要背：

| 角色 | 字体 | 为什么 |
| --- | --- | --- |
| serif | Songti SC（`STSongti-SC-Bold`） | 宋/明骨架，和设计稿用的 Noto Serif SC 同族 |
| sans | PingFang SC（`PingFangSC-Regular` / `-Medium`） | 系统中文无衬线，别的 Mac App 正文就长这样 |
| hand | Hanzipen SC（`HanziPenSC-W3`） | 真手写字形，系统自带 |

设计稿画在 Noto Serif SC / Noto Sans SC 上。打包它们要花 20-40MB 和一份 OFL 声明，
换来的差别在 40pt 上看得见、在 14pt 上几乎看不见。手写体本来是唯一看起来非打包不可的，
直到发现 macOS 本来就带了一款。

字重靠选具名字体（`STSongti-SC-Bold`、`PingFangSC-Medium`）而不是 `.weight()`：
在 CJK 字体上合成粗体会把笔画糊在一起。

**必须用精确 PostScript 名。** `Font.custom` 遇到不存在的名字会静默回退系统字体——
那正是「看起来没问题、实际没生效」的那类失败。四个名字都在本机验证过能解析，
且 `HanziPenSC-W3` 覆盖「今天清完了」全部字形。

### 系统层

| 样式 | 基准 | 用途 |
| --- | --- | --- |
| `mono` / `monoBody` | `.monospaced` | **机器产物**：run id、周期标签、文件路径、cron、模型名、token |
| `tabular*` | `monospacedDigit()` | 需要对齐的数字列 |
| `bodyStrong` | sans 14 / 500 | 今天页之外的列表行强调 |

等宽在这里是承重的：它是「这是系统产出的」和「这是你写的」之间的唯一视觉区分。
设计稿没给它名字，但删掉它不是精简字阶，是删掉产品依赖的一个区分——
和状态色是同一个判断。`bodyStrong` 同理：今天页不用它（那里的强调是位置），
但另外八个屏幕的列表行需要在不动用颜色的前提下说「这一条」。

---

## 4. 间距与形状

统一 4pt 网格。iOS 拿到的是更大的**点击区**，不是更大的间距——
布局的节奏在 Mac 和手机上应该读起来是同一个产品。

```
xxs 4   xs 8   sm 12   md 16   lg 24   xl 32   xxl 40
```

顶格从 48 降到 40：旧的那一档零调用，色阶在背一个没人用的值。

**圆角只有两种。**

| | 值 |
| --- | --- |
| 圆角 | `radiusPaper` 4（纸、面板，一切是表面的东西）/ `radiusPill` ∞（一切能按的东西） |
| 发丝线 | 1pt，永不更粗 |
| 圆圈描边 | 1.5pt。它是可操作提示不是分隔线，1pt 的 `ink3` 读起来像脏点 |
| 状态圆圈 | 22 |
| 最小点击区 | macOS 28 / iOS 44 |
| 正文最大宽度 | 720 |
| 侧栏 | 190 / 214 / 280 |
| 列表栏 | 260 / 320 / 420 |

旧的四级圆角收敛成两级：表面是 4，控件是全圆，中间那些值没人能说出选择规则。

### 迁移别名（临时）

`surface` / `surfaceSunken` / `inkMuted` / `line` / `moss` / `mossSoft` /
`radiusSmall` / `radiusMedium` / `radiusLarge` 仍然可用，指向各自的新 token。

两个原因：这两个仓库加起来 655 处调用还这么拼写，在同一个 commit 里既改值又改名会让 diff
没法评审——分不清哪处是有意改色、哪处是改名手滑；而且 daily-os-ios 依赖这个包却没法从这边构建，
硬改名会弄坏一个本 PR 无从验证的仓库。

**值立刻跟新色板走，只有拼写滞后。** 刻意没加 `@available(deprecated:)`：
那会为每一处调用产生一条警告（655 条），而一个永远打印 655 条警告的构建，
等于第 656 条真警告不可见。改名 PR 里再加这个标记，那时它只需要为真几小时。

---

## 5. 组件

系统里只有**一种容器**：`Panel`。表面 + 发丝线 + 标题行（可带尾部动作）。
没有 elevated / outlined 变体。八个屏幕全部由它搭出来，这是它们看起来像一个 App 的原因。

| 组件 | 说明 |
| --- | --- |
| `Panel` / `PanelDivider` | 唯一容器与其内部分隔线 |
| `ScreenScaffold` | 屏幕外壳：标题、副标题、滚动体。所有屏幕都用，标题就不会在页面间漂移几个点 |
| `Pill` | 状态标签。**只表达状态，不承载导航**——能点的是按钮不是徽章 |
| `StatusDot` | 色点 + 文字。颜色是信息，文字是确认 |
| `PixelAvatar` | 5×5 identicon，见 §8 |
| `MossButtonStyle` | 强调按钮。`prominent` 每个区域最多一个 |
| `QuietButtonStyle` | 面板头部的无边框动作 |
| `CheckCircle` | 待办勾选。刻意不用 `Toggle`（Mac 上渲染成复选框、iOS 上是开关，同一个列表需要同一个手势） |
| `ProgressTrack` | KR 进度、运行进度 |
| `EmptyState` | 图标 + 标题 + 一句解释 + 一个动作 |
| `StatTile` | 一格统计。三四格横排就是原来整个 Dashboard |
| `TwoColumns` | Mac 并排、手机堆叠 |
| `ListColumn` / `SelectableRow` | 屏内 list-detail 的左栏 |

**空状态永远不是空白页。** 说清原因，给出解决它的那一个动作。

---

## 6. 交互约定

**周期编辑（最重要的一条）**
一个周期是一个 markdown 文件，三段各自独立：要务 / retro / review。两件事必须始终可见：

1. **这一段是谁写的**——planner / 你 / AI。徽章常驻在每一段下方。
2. **planner 重跑不覆盖手工编辑。** 你改过的段落，planner 的新版本进入「待合入草稿」，
   顶部出现横幅，可以左右对比、合入或丢弃。静默覆盖会毁掉唯一一份副本。

**只读**
看队友的周期时，编辑控件**不存在**，不是禁用。只读是数据的属性，不是按钮的属性。
（服务端和数据库 RLS 各有一层强制，UI 这层是镜像。）

**保存反馈**
每一次写入都落到磁盘上的某个文件。Toast 里写出**具体路径**——
`已保存到 20_CYCLES/2026-08-24_8.24-9.6.md`。这是不喊叫地告诉用户"哪个文件变了"的唯一方式。

**排程**
节奏用人话（"每天 08:30"），cron 表达式降级为元信息但**不隐藏**——
第一次要排查任务没触发时，藏起来就是撒谎；而以 `30 8 * * *` 开头的界面是需要先解码才能用的界面。

**破坏性动作**
用 `danger` tone 的 `QuietButtonStyle`，永远不做成 prominent 填充按钮。

**任务行：先选中，再操作**（`TaskRow`）
今天页两列的行，之前每行常驻 2–3 个文字按钮。理由写得挺好听——「要去找的控件就是不会被用的控件」——
但那句话成立的前提是**只在 hover 时出现**，而这里不是。同一个操作有四条路：

| 方式 | 用途 |
| --- | --- |
| 点一下行 → 图标出现 | 主路径，鼠标 |
| hover → 图标出现 | 路过时顺手 |
| 右键 → 带文字的菜单 | 图标记不住时 |
| 选中后按键（⏎ 完成 / E 修改 / D 顺延 / R 恢复 / ⌫ 删除） | 键盘流 |

图标区的宽度**始终占位**，藏起来的时候也是——行在指针底下横向重排是误点的头号来源。
图标只有图标，但 `label` 同时是 tooltip、右键菜单文字和 VoiceOver 名字，这才是允许纯图标存在的前提。

选中是**整屏一个**，不是每个面板一个：键盘作用于「选中的那行」，这句话必须只有一个答案。

**每个操作配一个动画，动画就是回执**
- **完成**：勾先弹进去（spring），停 320ms，行再离开。同一帧消失读起来是「行不见了」，不是「我做完了」——
  而在一个同时能删除条目的列表里，这两件事必须不一样。
- **顺延 / 恢复**：`.snappy`，行就地换状态。
- **删除**：确认之后行高塌陷，下面的行合拢上来。淡出会留一帧空洞再吸走，像卡了一下。
- **改估时**：上方那条比例条跟着重画。**改这个数字的全部意义就是看今天的形状变化。**

`TaskRow` 不认识 `AppState`——它收 `[TaskAction]` 和闭包。所以 preview 里能单独看，两列能共用一份。

**今日进度条**（`PlanSegment`）
一格一件事，不是连续百分比——一天是可数的，把它画成连续的，会藏住「最后 20% 是一整件事」。
三条规则：

1. **做完的往左移。** 一个你会不断重排的列表，如果完成的格子散落在中间，进度就读不出来：
   眼睛从左往右读，要的是「身后」和「身前」之间**一条边界**，不是五条。
2. **宽度跟着估时走。** 等宽格子会说「还剩四件」，而其中三件是十分钟、第四件是一下午。
3. **没估时的照样画一格**，借用其他项的**中位数**（不是平均数——一个三小时的异常值会把所有未知格子撑长）。
   借来的宽度画虚线边框，**但只在混合时**：全都没估时的时候，等宽不是猜测，是「对时长一无所知」的诚实画法。

顺延排在最后而不是混在未完成里——它不是今天等着你的活，留在原位会让剩下的条看起来比一天还长。

排序和宽度抽成了 `PlanSegment.layout`，由 `daily-os-checks` 覆盖。理由和环形图一样：
**一排彩色方块看起来永远像一排彩色方块**，顺序错了、或者一个猜出来的宽度和旁边真实的数字长得一样权威，
在屏幕上都不会喊出来。

**图表**（`DonutChart` / `TrendLine`）
手绘，不用 Swift Charts。两个图各自只是一个形状加几个标签，而它们要坐在一个整套视觉论据
就是发丝线和暖纸底的面板里——引进一个图表框架，光是覆盖它的网格线、字体和图例，
写的代码就比形状本身多，最后还是一个「贴进 App 的图表」而不是 App 的一部分。

`Palette.series` 和状态色一样，是**「只有一个色相」这条规矩的明确例外**（见 §2）。
那条规矩是为了让一屏控件不像老虎机，但一个五片都是同一种薄荷深浅的饼图是**读不出来的**
——每一片都得回去对一次图例。分类数据需要能区分的色相。约束还在：全部降饱和度以坐在纸底上，
`mint400` 排第一（于是最大的那片通常还是 accent），只有六个
（需要第七个的图表，是应该把长尾归成「其他」的图表）。

两个图都**必须能表达「没有数据」**，而且这不是罕见状态：
- 环形图——模型判断不出来就不给 `minutes`，改提示词之前跑的计划一条都没有
- 曲线图——手工维护的 vault 里，大多数周期的要务根本没标过状态

「没标记」和「0%」是两回事。把没标记的周期画成 0%，是**凭空画出一次崩盘**。
所以 `Cycle.completion` 是 optional，`PrioritiesDocument.markedCount` 是它的闸门。

角度算术抽成了 `DonutArc.layout`，由 `daily-os-checks` 覆盖。
因为**环形图是唯一会「安静地」错的东西**：环看起来永远像个环，
弧加起来不满一圈、或者一小片被自己的间隔吃掉，在屏幕上都不会喊出来。

---

## 7. 平台差异清单

| | macOS | iOS |
| --- | --- | --- |
| 导航 | 两栏 split view + 侧栏分组 | 3 tab + 更多 |
| List-detail | 屏内左右分栏 | `NavigationLink` 推入 |
| 配置 | 完整设置（owner-only） | 只读状态 |
| 菜单栏 / 快捷捕获 | `MenuBarExtra` | Today 页顶部输入框 |
| 快捷键 | ⌘1…⌘7、⌘, | — |
| 对话列表 | 常驻左栏 | 工具栏按钮 → sheet |
| 最小点击区 | 28 | 44 |

---

## 8. 像素头像

5×5 identicon，左半格生成后镜像。它是 daily-os 服务里 `src/ui/avatar.ts` 的**逐位移植**：
FNV-1a 和 mulberry32 原样照抄，没有换成更 Swift 的写法。

理由：同一个账号必须在 Mac、手机和浏览器上画出**完全一样**的头像。
一个在两个端长得不一样的头像，比没有头像更糟。

`swift run daily-os-checks` 用真实 JS 实现的输出做校验（不是从算法重新推导），
四个种子的格子和色相 + 400 个生成种子的"既不空也不满"约束。

---

## 9. 可访问性

- 所有文字走系统字体样式 → 动态字体自动生效
- 颜色永不单独承载信息：状态点必配文字，pill 必有文案，进度条必有百分比
- 最小点击区按平台设定，`CheckCircle` 的可点区域是 44×44 而不是视觉上的 18×18
- `EmptyState` / `PixelAvatar` / `ProgressTrack` 带 accessibility label 或 value

## 10. 还没做的

**两端共同**

- 图标与 App Icon（企鹅资产要重画成 macOS 26 / iOS 26 的图标规格）
- 动效规范（目前只有 toast 的淡入淡出）
- Shortcuts / App Intents（快捷捕获应该能从 Siri 和快捷指令进）

**macOS（本仓库）**

- 签名、公证、分发、自动更新——目前 `.app` 是未签名的，别人下载后打不开
- 打印 / 导出周期为 PDF
- 拖拽：把文件拖进快捷捕获、把周期拖出成 markdown

**iOS（另一个仓库）**

- Widget 与 Live Activity（"进行中的 run" 天然适合 Live Activity）
- 服务发现：手机怎么找到 Mac 上的服务（Bonjour？手动填地址？两者都要有降级）
